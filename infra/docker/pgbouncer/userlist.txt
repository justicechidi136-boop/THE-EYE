; PgBouncer user credentials. Replace the password hash in production.
; Generate with: docker exec the-eye-postgres-postgis psql -U the_eye -d the_eye -c "SELECT concat('\"', usename, '\" \"', passwd, '\"') FROM pg_shadow WHERE usename = 'the_eye';"
"the_eye" "change_me_postgres"
