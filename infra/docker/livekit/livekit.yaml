port: 7880
bind_addresses:
  - 0.0.0.0
rtc:
  tcp_port: 7881
  udp_port: 7882
  # node_ip patched at deploy (LIVEKIT_NODE_IP or public IP auto-detect).
  use_external_ip: false
logging:
  level: info
