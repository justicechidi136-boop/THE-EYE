/// Confirmed brand colors sampled from the official Field Ops logo artwork.
///
/// Orange ≈ #FF9934 and green ≈ #009934 — already represented by [FieldColors]
/// tokens (`orange` / `success`). Prefer those existing tokens in UI code.
abstract final class FieldBrandArtworkColors {
  static const orange = 0xFFFF9934;
  static const green = 0xFF009934;
}
