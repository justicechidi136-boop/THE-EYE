<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow HTTP to a dev machine (localhost via adb reverse or LAN IP). -->
    <application android:usesCleartextTraffic="true" />
</manifest>
