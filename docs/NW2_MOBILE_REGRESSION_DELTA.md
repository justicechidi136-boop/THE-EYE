# NW(2) Mobile Regression Delta

Baseline: `97040b51ec1f6d4d7ac48e824a34ee950b09e79f`

| ID | Current behavior | Previous implementation | Still present? | Runtime/config dependency | Root cause and classification | Files/tests affected | Exact remediation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| NW2-001 | The Feed engagement row can place Comment on a different visual baseline from Like and Share when labels consume different widths. | Neighborhood Watch Feed already provides Like, Comment, and Share actions and working handlers. | Yes. | None. | The row uses three independent `TextButton.icon` widgets with dynamic labels in equal thirds, without a shared one-line action layout. Comment can wrap or size differently on constrained displays. **INCOMPLETE PREVIOUS FIX / FRONTEND UI ISSUE.** | `apps/mobile/lib/main.dart`; Neighborhood Watch widget tests. | Introduce one shared post-action widget with a stable accessible height, centered icon/label layout, one-line fitted label, and consistent spacing; use it for all three actions and test narrow/large-text layouts. |
| NW2-002 | Physical QA reports that retrying selected evidence still fails after the binary upload. | Shared capture, validation, Firebase V4 signed PUT, incident confirmation, retry state, and UI refresh are present. PR #173 added `71a71a22` to serialize Prisma `BigInt`/numeric fields returned by incident-media confirmation. | Yes, and staging now runs the PR #173 merge SHA. | Requires the deployed staging API/Firebase configuration and an authenticated device session. | The original confirmation created the media row before its response failed serialization. A retry signs and uploads the same bytes under a new random object key, but confirmation deduplicates by file hash and then rejects the new key even though the canonical row belongs to the same incident. This leaves the UI in failed/retry state. **BACKEND API IDEMPOTENCY ISSUE.** | Incident media confirmation service/test; existing mobile evidence retry tests; physical photo/video/audio and retry QA. | Treat `incidentId + fileHash` as the retry identity: return the canonical private media row when it belongs to the same incident and bucket, while retaining cross-incident/bucket conflict protection. Preserve Firebase architecture and signed/private object rules. |
| NW2-003 | Manual Stolen Vehicle entry previously painted only the VIN field and made the rest of the form appear unusable. | Make, model, year, color, plate, VIN, photos, last-seen date/time/location, theft description, distinguishing features, evidence, payload mapping, DTO, persistence, and detail rendering all remain present. | Yes. The rebuilt APK now renders the full form on V2322; live screenshots and the accessibility tree confirm all six vehicle inputs plus the later sections. | None. | `VehiclePhotoSection`, introduced by the latest vehicle-photo work, placed a globally full-width themed `FilledButton` inside an unconstrained `Row`. Device logcat proved `BoxConstraints(w=Infinity)` followed by cascading render failures. **REGRESSION / FRONTEND UI ISSUE, FIX VERIFIED ON DEVICE.** | `apps/mobile/lib/vehicles/vehicle_photo_section.dart`; vehicle-photo and Stolen Vehicle widget tests. | Keep the finite horizontal button constraint and real-theme regression coverage. The form is scrollable; Make through Plate are above VIN and the remaining sections are below it. |
| NW2-004 | QA reported return to Login after closing/reopening the app. | Remain Signed In defaults ON; login intent, persistent backend refresh session, encrypted token storage, refresh rotation, single-flight 401 retry, transient-failure preservation, and manual logout remain implemented and covered. | Yes. | A fresh session must be issued after the persistent-auth rollout; a revoked/expired old refresh session cannot be upgraded locally. | The previously retained device session was no longer present/renewable. After a fresh login, presence-only checks confirm both encrypted access and refresh token keys survive process stop, while the preference remains default ON. No source overwrite or TTL regression was found. **STALE RUNTIME SESSION / RUNTIME VALIDATION ISSUE.** Fresh-session close/reopen, authenticated action, silent refresh, offline/reconnect, and manual logout still require final device certification. | Existing auth persistence, restore, retry, rotation, and API auth tests; physical fresh-session QA. | Do not change TTL or auth architecture. Validate the newly issued persistent session end to end; change source only if that controlled run fails with a specific refresh/storage defect. |

## Preservation decisions

- Existing Firebase Storage, evidence authorization, retry behavior, and private-object rules remain unchanged.
- Existing persistent-session and manual-sign-out architecture remains unchanged pending controlled fresh-session QA.
- Existing Stolen Vehicle fields and payload contracts are repaired at the layout constraint only; no field set or DTO is being reinvented.
